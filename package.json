{
  "name": "train-buddy-whatsapp-bot",
  "version": "1.0.0",
  "description": "WhatsApp Bot Integration for Train Buddy AI Assistant",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "npx puppeteer browsers install chrome",
    "start": "node --expose-gc --env-file=.env index.js"
  },
  "dependencies": {
    "whatsapp-web.js": "^1.23.0",
    "qrcode-terminal": "^0.12.0"
  }
}
